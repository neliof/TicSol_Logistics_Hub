---
# This is a TEMPLATE. DO NOT COMMIT ACTUAL SECRETS.
# Use one of these methods instead:
#
# Option 1: kubectl create secret generic ticsol-secrets \
#   --from-literal=db-password=YOUR_PASSWORD \
#   --from-literal=jwt-secret=YOUR_JWT_SECRET \
#   --from-literal=artsoft-user=YOUR_USER \
#   --from-literal=artsoft-password=YOUR_PASSWORD \
#   -n ticsol-logistics
#
# Option 2: Use Sealed Secrets (recommended for GitOps)
# https://github.com/bitnami-labs/sealed-secrets
#
# Option 3: Use External Secrets Operator with your secret backend
# https://external-secrets.io/

apiVersion: v1
kind: Secret
metadata:
  name: ticsol-secrets
  namespace: ticsol-logistics
type: Opaque
stringData:
  # Database
  db-password: "CHANGE_ME_IN_PRODUCTION"

  # JWT
  jwt-secret: "CHANGE_ME_LONG_RANDOM_STRING_MIN_32_CHARS"

  # ARTSOFT
  artsoft-user: "admin"
  artsoft-password: "CHANGE_ME_IN_PRODUCTION"
